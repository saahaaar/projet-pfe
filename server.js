/*const express = require('express');
const path = require('path');
const app = express();

app.get('/getData', (req,res)=>{
  res.json(
   "hello world"
  )
})
app.listen(4600, (req,res)=> {
  console.log('RUNNING');
});
*/ 